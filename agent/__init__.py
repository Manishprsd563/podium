"""Podium's LiveKit agent: the voice session orchestrator (`session_agent.py`),
session persistence (`store.py`), the temporal coaching graph (`graph.py`),
LLM role wiring (`llm_config.py`), and the standalone plugin-wiring smoke test
(`gate0_agent.py`). See CONTRACTS.md for the wire formats these modules
implement.
"""
